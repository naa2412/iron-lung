[build]
builder = "nixpacks"

[deploy]
startCommand = "npx prisma generate && npx prisma migrate deploy && npx prisma db seed && node src/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
